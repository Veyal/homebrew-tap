# Homebrew tap — Interseptor

```bash
brew install Veyal/tap/interseptor
```

Formulae are published from [Veyal/interseptor](https://github.com/Veyal/interseptor) releases when `HOMEBREW_TAP_TOKEN` is configured.
